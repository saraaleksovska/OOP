 Client &operator+=(StockRecord &sr){
        StockRecord *tmp=new StockRecord[n+1];
        for(int i=0; i<n; i++){
            tmp[i]=niza[i];
        }
        kompanii[n++]=sr;
        delete[] niza;
        niza=tmp;
        return *this;
    }
